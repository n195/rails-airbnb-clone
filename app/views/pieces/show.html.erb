<h2><%= @piece.name %></h2>
<p><%= @piece.price %></p>
<p><%= @piece.creation_date %></p>
<p><%= @piece.description %></p>
<%= link_to "Delete", piece_path(@piece), method: :delete, class: "btn btn-ghost" %>
<%= link_to "Edit", edit_piece_path(@piece), method: :get, class: "btn btn-flat" %>
