<div class="container max-height-100vh">
  <h1>Bienvenue sur My MasterPiece 🎨<h1>
  <div class="btn"> <%= link_to "Mettre une oeuvre en location", new_piece_path(@piece)%> </div>
  <div class="row">
    <% 12.times do %>
      <%= render "shared/card_piece" %>
    <% end %>
  </div>
</div>
